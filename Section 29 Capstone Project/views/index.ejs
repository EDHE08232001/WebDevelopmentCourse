<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Weather App</title>
    <link rel="stylesheet" href="/style.css">
</head>
<body>
    <h1>Weather Dashboard</h1>
    <div>
        <% if (weather) { %>
            <h2>Current Weather for <%= weather.name %>, <%= weather.sys.country %></h2>
            <div class="weather-details">
                <p><strong>Temperature:</strong> <%= (weather.main.temp - 273.15).toFixed(2) %> °C (Feels like: <%= (weather.main.feels_like - 273.15).toFixed(2) %> °C)</p>
                <p><strong>Weather:</strong> <%= weather.weather[0].main %> - <%= weather.weather[0].description %></p>
                <p><strong>Wind:</strong> Speed: <%= weather.wind.speed %> m/s, Direction: <%= weather.wind.deg %> degrees</p>
                <p><strong>Cloudiness:</strong> <%= weather.clouds.all %>%</p>
                <p><strong>Pressure:</strong> <%= weather.main.pressure %> hPa</p>
                <p><strong>Humidity:</strong> <%= weather.main.humidity %>%</p>
                <p><strong>Visibility:</strong> <%= weather.visibility %> meters</p>
                <p><strong>Sunrise:</strong> <%= new Date(weather.sys.sunrise * 1000).toLocaleTimeString() %></p>
                <p><strong>Sunset:</strong> <%= new Date(weather.sys.sunset * 1000).toLocaleTimeString() %></p>
            </div>
        <% } %>
    </div>
    <button onclick="location.href='/'">Refresh</button>
</body>
</html>
